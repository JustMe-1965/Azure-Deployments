name: Terraform Deployment
on: push
jobs:
  deploy:
    env:
      AZURE_ACCESS_KEY_ID: "${{ secrest.AZURE_ACCESS_KEY_ID }}"
    runs-on: Canonical-latest
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.12.2
          
